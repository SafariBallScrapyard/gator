# gator
For the purpose of Boot.dev coursework.

This repo requires both Postgress and Golang to run.
You can install Gator by running `go install` in command line.
You will need to register a username through the `register` command when starting this CLI.
